# Phase 1: v0.7 Feel Good 実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.7 Feel Good Update を完全実装し、Pixel Bloom が触覚・視覚・節目演出の全層で App Store v1.0 リリース可能な状態にする。

**Architecture:** 既存設計仕様 `docs/superpowers/specs/2026-04-21-v0.7-feel-good-update-design.md` を 1 行も省略せず採用。M1 (触覚底上げ) → M2 (完了儀式中核) → M3 (マイルストーン演出) の順で実装。各 M に独立したコミット可能な完了条件を持つ。

**Tech Stack:** SwiftUI / SwiftData / Swift 6 / iOS 17+ / Xcode Testing Framework / Canvas+TimelineView (パーティクル)

---

## 前提

- 仕様書 `docs/superpowers/specs/2026-04-21-v0.7-feel-good-update-design.md` を完全に参照する
- 現状の未コミット差分 (Localizable.xcstrings 等 8 ファイル) はこのプラン開始前に整理する
- ビルド/テストコマンド (CODE_SIGNING_ALLOWED=NO):

```bash
xcodebuild test \
  -scheme PixelColoringGame \
  -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator \
  -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived \
  CODE_SIGNING_ALLOWED=NO
```

```bash
xcodebuild build \
  -scheme PixelColoringGame \
  -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived \
  CODE_SIGNING_ALLOWED=NO
```

---

## File Structure

新規ファイル:

```
PixelColoringGame/Features/Celebration/
├── CelebrationEvent.swift            # enum + priority/seenKey/supersedes
├── CelebrationDetector.swift         # 純関数
├── CelebrationCoordinator.swift      # ObservableObject
├── CelebrationFlowState.swift        # CompletionView ローカル
├── ParticleEmitterView.swift         # Canvas+TimelineView, .confetti
├── ChapterClearView.swift
├── JourneyCompleteView.swift
├── MonthlyCompleteView.swift
├── MilestoneToastView.swift
└── MilestoneToastHost.swift

PixelColoringGame/Utilities/
└── AnimationProfile.swift            # Reduce Motion 統一ヘルパ

PixelColoringGame/Features/Debug/
└── DebugMenuView.swift               # #if DEBUG

PixelColoringGameTests/Celebration/
├── CelebrationDetectorTests.swift
├── CelebrationCoordinatorTests.swift
├── CelebrationsSeenStorageTests.swift
└── AnimationProfileTests.swift
```

修正対象既存ファイル:

```
PixelColoringGame/Utilities/HapticFeedbackRateLimiter.swift  # 5段階化
PixelColoringGame/Features/Game/PixelBoardView.swift          # セル塗りpop、ヒントpulse
PixelColoringGame/Features/Game/GameView.swift                # haptic呼び出し更新、間違いタップerror tone
PixelColoringGame/Features/Game/PaletteTrayView.swift         # 色完了 scale+glow
PixelColoringGame/Features/Completion/CompletionView.swift    # cascade+confetti
PixelColoringGame/App/AppView.swift                           # CelebrationCoordinator注入、root overlay/cover
PixelColoringGame/Core/GameSessionStore.swift                 # finishLevel末尾でhandle(trigger:)
PixelColoringGame/Core/DailyExperience.swift                  # commitToday末尾でhandle(trigger:)
PixelColoringGame/Models/PlayerProfile.swift                  # celebrationsSeenData属性追加
PixelColoringGame/Features/Title/TitleScreenView.swift        # #if DEBUG version長押し
```

---

## Section M1: Layer 1 触覚底上げ (約 2 日)

**M1 ゴール**: セル塗りpop、ハプティック5段階、パレット色完了演出、ヒントセルpulse、間違いタップsoft tone。新規ディレクトリ不要、既存ファイル変更のみ。

### Task M1.1: 未コミット差分整理

**Files:**
- Read all modified files first

- [ ] **Step 1: 未コミット差分を全部 git diff で確認**

```bash
git diff --stat
git diff --color=never PixelColoringGame/Features/Daily/DailyChallengeHeroCard.swift
git diff --color=never PixelColoringGame/Features/Collection/CollectionBookView.swift
git diff --color=never PixelColoringGame/Features/Completion/CompletionView.swift
git diff --color=never PixelColoringGame/Features/Journey/EventDetailView.swift
git diff --color=never PixelColoringGame/Features/Journey/EventPresentationComponents.swift
```

期待: ローカライズキー切替、テスト追加が中心であることを確認。

- [ ] **Step 2: xcstrings の差分は維持、その他は維持で OK と判断**

未コミット差分は v0.7 実装の障害にならないので、そのままコミット可。

- [ ] **Step 3: 整理コミット**

```bash
git add PixelColoringGame/Features/Daily/DailyChallengeHeroCard.swift \
        PixelColoringGame/Features/Collection/CollectionBookView.swift \
        PixelColoringGame/Features/Completion/CompletionView.swift \
        PixelColoringGame/Features/Journey/EventDetailView.swift \
        PixelColoringGame/Features/Journey/EventPresentationComponents.swift \
        PixelColoringGame/Resources/Localizable.xcstrings \
        PixelColoringGameTests/DailyExperienceTests.swift \
        PixelColoringGameTests/GameSessionStoreTests.swift \
        PixelColoringGameTests/LocalizationTests.swift
git commit -m "chore: pre-v0.7 cleanup (localization keys & test additions)"
```

`.DS_Store` と `xcuserstate` はコミットしない (gitignore 推奨だがスコープ外)。

### Task M1.2: AnimationProfile を新規作成 (TDD)

**Files:**
- Create: `PixelColoringGame/Utilities/AnimationProfile.swift`
- Create: `PixelColoringGameTests/Celebration/AnimationProfileTests.swift`

- [ ] **Step 1: テストディレクトリ作成 + 失敗するテストを書く**

`PixelColoringGameTests/Celebration/AnimationProfileTests.swift`:

```swift
import XCTest
@testable import PixelColoringGame

final class AnimationProfileTests: XCTestCase {
    func test_default_shouldShowParticles_true() {
        let profile = AnimationProfile(reduceMotion: false, lowPower: false)
        XCTAssertTrue(profile.shouldShowParticles)
    }

    func test_reduceMotion_shouldShowParticles_false() {
        let profile = AnimationProfile(reduceMotion: true, lowPower: false)
        XCTAssertFalse(profile.shouldShowParticles)
    }

    func test_lowPower_reducesParticleBudget() {
        let normal = AnimationProfile(reduceMotion: false, lowPower: false)
        let low = AnimationProfile(reduceMotion: false, lowPower: true)
        XCTAssertEqual(normal.particleBudget, 80)
        XCTAssertLessThan(low.particleBudget, normal.particleBudget)
    }

    func test_cascadeDelay_normalMode_returnsIndexTimes015() {
        let profile = AnimationProfile(reduceMotion: false, lowPower: false)
        XCTAssertEqual(profile.cascadeDelay(0), 0.0, accuracy: 0.001)
        XCTAssertEqual(profile.cascadeDelay(2), 0.30, accuracy: 0.001)
    }

    func test_cascadeDelay_reduceMotion_returnsZero() {
        let profile = AnimationProfile(reduceMotion: true, lowPower: false)
        XCTAssertEqual(profile.cascadeDelay(0), 0.0, accuracy: 0.001)
        XCTAssertEqual(profile.cascadeDelay(5), 0.0, accuracy: 0.001)
    }
}
```

- [ ] **Step 2: テスト実行で失敗を確認**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO \
  -only-testing:PixelColoringGameTests/AnimationProfileTests 2>&1 | tail -20
```

期待: `AnimationProfile` 未定義でコンパイルエラー。

- [ ] **Step 3: AnimationProfile 実装**

`PixelColoringGame/Utilities/AnimationProfile.swift`:

```swift
import SwiftUI

struct AnimationProfile {
    let reduceMotion: Bool
    let lowPower: Bool

    static var current: AnimationProfile {
        AnimationProfile(
            reduceMotion: UIAccessibility.isReduceMotionEnabled,
            lowPower: ProcessInfo.processInfo.isLowPowerModeEnabled
        )
    }

    var shouldShowParticles: Bool {
        !reduceMotion
    }

    var particleBudget: Int {
        if !shouldShowParticles { return 0 }
        return lowPower ? 24 : 80
    }

    func cascadeDelay(_ index: Int) -> Double {
        reduceMotion ? 0 : 0.15 * Double(index)
    }

    func pop() -> Animation {
        reduceMotion
            ? .easeOut(duration: 0.10)
            : .spring(response: 0.25, dampingFraction: 0.55)
    }

    func tintTransition() -> Animation {
        reduceMotion
            ? .easeOut(duration: 0.15)
            : .easeInOut(duration: 0.25)
    }
}
```

- [ ] **Step 4: テストがパスすることを確認**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO \
  -only-testing:PixelColoringGameTests/AnimationProfileTests 2>&1 | tail -10
```

期待: `Test Suite 'AnimationProfileTests' passed`

- [ ] **Step 5: コミット**

```bash
git add PixelColoringGame/Utilities/AnimationProfile.swift \
        PixelColoringGameTests/Celebration/AnimationProfileTests.swift
git commit -m "feat(M1): add AnimationProfile for reduce-motion-aware animations"
```

### Task M1.3: HapticFeedbackRateLimiter を 5 段階化 (TDD)

**Files:**
- Modify: `PixelColoringGame/Utilities/HapticFeedbackRateLimiter.swift`
- Modify: `PixelColoringGame/Features/Game/GameView.swift:153,156,161,169,174`

仕様書 §4 「ハプティック階層化」: 5段階 (cellFill 軽 / colorComplete 中 / levelComplete 強 / chapterClear notif success / milestone 軽 notif)。各段階独立レートリミッタ。

- [ ] **Step 1: 既存呼び出し箇所を確認**

```bash
grep -n "feedback\." PixelColoringGame/Features/Game/GameView.swift
```

期待: 153 `incorrectTap`, 156 `correctTap`, 161 `success`, 169 `correctTap`, 174 `success`。

- [ ] **Step 2: HapticFeedbackRateLimiter を新 API で書き直す**

`PixelColoringGame/Utilities/HapticFeedbackRateLimiter.swift` を以下に置換:

```swift
import UIKit

@MainActor
final class HapticFeedbackRateLimiter {
    enum Stage {
        case cellFill       // 軽: セル塗り
        case colorComplete  // 中: 色完了
        case levelComplete  // 強: レベル完了
        case chapterClear   // 通知success: 章クリア
        case milestone      // 軽 通知: マイルストーントースト
        case incorrectTap   // 既存互換: 間違いタップ
    }

    private struct StageState {
        let minimumInterval: TimeInterval
        var lastTriggerDate: Date = .distantPast
    }

    private var states: [Stage: StageState] = [
        .cellFill: StageState(minimumInterval: 0.08),
        .colorComplete: StageState(minimumInterval: 0.20),
        .levelComplete: StageState(minimumInterval: 0.40),
        .chapterClear: StageState(minimumInterval: 0.50),
        .milestone: StageState(minimumInterval: 0.30),
        .incorrectTap: StageState(minimumInterval: 0.16),
    ]

    private let lightImpact = UIImpactFeedbackGenerator(style: .light)
    private let softImpact = UIImpactFeedbackGenerator(style: .soft)
    private let mediumImpact = UIImpactFeedbackGenerator(style: .medium)
    private let rigidImpact = UIImpactFeedbackGenerator(style: .rigid)
    private let notification = UINotificationFeedbackGenerator()

    init() {
        [lightImpact, softImpact, mediumImpact, rigidImpact].forEach { $0.prepare() }
        notification.prepare()
    }

    func fire(_ stage: Stage) {
        guard var state = states[stage] else { return }
        let now = Date()
        guard now.timeIntervalSince(state.lastTriggerDate) >= state.minimumInterval else { return }
        state.lastTriggerDate = now
        states[stage] = state

        switch stage {
        case .cellFill:
            lightImpact.impactOccurred(intensity: 0.55)
        case .colorComplete:
            mediumImpact.impactOccurred(intensity: 0.8)
        case .levelComplete:
            rigidImpact.impactOccurred(intensity: 1.0)
        case .chapterClear:
            notification.notificationOccurred(.success)
        case .milestone:
            notification.notificationOccurred(.success)
        case .incorrectTap:
            softImpact.impactOccurred(intensity: 0.75)
        }
    }

    // Legacy compatibility wrappers (will be removed once all callers migrated)
    func incorrectTap() { fire(.incorrectTap) }
    func correctTap() { fire(.cellFill) }
    func success() { fire(.levelComplete) }
}
```

注: 後方互換の wrapper を残しているのは GameView 既存呼び出し更新の中間ステップ。Task M1.4 で完全に新 API に置き換える。

- [ ] **Step 3: ビルド確認**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

期待: BUILD SUCCEEDED (互換 API があるため既存呼び出しは動く)。

- [ ] **Step 4: コミット**

```bash
git add PixelColoringGame/Utilities/HapticFeedbackRateLimiter.swift
git commit -m "feat(M1): 5-stage haptic rate limiter (cellFill/colorComplete/levelComplete/chapterClear/milestone)"
```

### Task M1.4: GameView の haptic 呼び出しを新 API に更新

**Files:**
- Modify: `PixelColoringGame/Features/Game/GameView.swift`

- [ ] **Step 1: GameView 全体を Read してcontext取得**

該当行を含むメソッド単位で確認。Task M1.3 で確認した行番号: 153, 156, 161, 169, 174。

- [ ] **Step 2: 各呼び出しを `feedback.fire(.xxx)` に置換**

```
feedback.incorrectTap()         → feedback.fire(.incorrectTap)
feedback.correctTap()           → feedback.fire(.cellFill)
feedback.success()              → feedback.fire(.levelComplete)  // 文脈に応じて colorComplete も検討
```

実コードを Read してから判断:
- 153: 間違いタップ時 → `.incorrectTap`
- 156: 正解タップ時 → `.cellFill`
- 161, 174: 色完了/レベル完了の文脈で `.colorComplete` or `.levelComplete`。コードの周辺を見て分岐
- 169: タップ後の追加フィードバック → `.cellFill` か `.colorComplete`

実装時にGameView前後 30 行を Read して各 call site の文脈を把握してから置換すること。

- [ ] **Step 3: ビルドとテスト**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

期待: BUILD SUCCEEDED。

- [ ] **Step 4: 旧 API ラッパー削除**

HapticFeedbackRateLimiter から `incorrectTap()`, `correctTap()`, `success()` の互換 wrapper を削除。

- [ ] **Step 5: 再ビルド + 全テスト**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

期待: 全テスト pass。

- [ ] **Step 6: コミット**

```bash
git add PixelColoringGame/Features/Game/GameView.swift \
        PixelColoringGame/Utilities/HapticFeedbackRateLimiter.swift
git commit -m "feat(M1): migrate GameView to 5-stage haptic API, drop legacy wrappers"
```

### Task M1.5: PixelBoardView にセル塗りpopアニメ追加

**Files:**
- Modify: `PixelColoringGame/Features/Game/PixelBoardView.swift:132-205`

仕様書 §3 Layer 1: 「正解タップで scale pop (1.0→1.12→1.0、0.25s) + opacity fade-in」。Reduce Motion 時は opacity fade のみ。

- [ ] **Step 1: PixelBoardCellView (132-205行) を Read**

該当行の現状 View 構造を理解する。`session.cellState(at:)` 等の API を確認。

- [ ] **Step 2: pop アニメ用 @State + onChange を追加**

PixelBoardCellView に以下を追加:

```swift
@Environment(\.accessibilityReduceMotion) private var reduceMotion
@State private var popScale: CGFloat = 1.0
@State private var lastFilled: Bool = false

private var profile: AnimationProfile {
    AnimationProfile(reduceMotion: reduceMotion, lowPower: ProcessInfo.processInfo.isLowPowerModeEnabled)
}
```

`body` 内の塗り表示要素に:

```swift
.scaleEffect(popScale)
.onChange(of: session.isFilledCorrectly(at: cellIndex)) { _, newValue in
    guard newValue, !lastFilled else { lastFilled = newValue; return }
    lastFilled = newValue
    guard !reduceMotion else { return }
    withAnimation(profile.pop()) { popScale = 1.12 }
    DispatchQueue.main.asyncAfter(deadline: .now() + 0.15) {
        withAnimation(profile.pop()) { popScale = 1.0 }
    }
}
```

注: 実 API は `session.isFilledCorrectly(at:)` か `session.cellState(at:).isFilled` か等、コード読みで確定する。

- [ ] **Step 3: ビルド**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

期待: BUILD SUCCEEDED.

- [ ] **Step 4: シミュレータで実機目視**

```bash
xcrun simctl boot 'iPhone 15' 2>/dev/null || true
open -a Simulator
xcrun simctl install booted /tmp/PixelColoringGameDerived/Build/Products/Debug-iphonesimulator/PixelColoringGame.app
xcrun simctl launch booted $(plutil -extract CFBundleIdentifier raw \
  /tmp/PixelColoringGameDerived/Build/Products/Debug-iphonesimulator/PixelColoringGame.app/Info.plist)
```

任意の Journey レベルでセルをタップして pop が見えるか確認。Reduce Motion を設定で ON にしても破綻しないか確認。

- [ ] **Step 5: コミット**

```bash
git add PixelColoringGame/Features/Game/PixelBoardView.swift
git commit -m "feat(M1): add cell fill pop animation with reduce-motion fallback"
```

### Task M1.6: パレット色完了 scale+glow 演出

**Files:**
- Modify: `PixelColoringGame/Features/Game/PaletteTrayView.swift:46-93`

仕様書 §3: 「色完了時にチップが scale+glow、完了 tint へ滑らかに移行」。

- [ ] **Step 1: PaletteTrayView (46-93 行) を Read**

現状の color chip 表示 View を理解する。完了状態の判定 API (例: `session.isColorComplete(colorIndex)`) を確認。

- [ ] **Step 2: 色チップに完了 transition を追加**

色チップ View の中に:

```swift
@Environment(\.accessibilityReduceMotion) private var reduceMotion
@State private var celebrateScale: CGFloat = 1.0
@State private var glowAmount: CGFloat = 0.0

private var profile: AnimationProfile {
    AnimationProfile(reduceMotion: reduceMotion, lowPower: ProcessInfo.processInfo.isLowPowerModeEnabled)
}
```

`onChange(of: session.isColorComplete(palette.index))`:

```swift
.onChange(of: session.isColorComplete(paletteIndex)) { _, newValue in
    guard newValue else { return }
    if reduceMotion {
        withAnimation(profile.tintTransition()) {
            // tint は既存仕組みで切り替わる
        }
    } else {
        withAnimation(profile.pop()) {
            celebrateScale = 1.18
            glowAmount = 1.0
        }
        DispatchQueue.main.asyncAfter(deadline: .now() + 0.20) {
            withAnimation(.easeOut(duration: 0.25)) {
                celebrateScale = 1.0
                glowAmount = 0.0
            }
        }
    }
}
.scaleEffect(celebrateScale)
.shadow(color: paletteColor.opacity(0.7 * glowAmount), radius: 12 * glowAmount)
```

- [ ] **Step 3: ビルド**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

- [ ] **Step 4: シミュレータで色完了させて目視確認**

任意の Journey レベルで 1 色を塗り切り、チップの scale+glow を確認。

- [ ] **Step 5: コミット**

```bash
git add PixelColoringGame/Features/Game/PaletteTrayView.swift
git commit -m "feat(M1): palette chip scale+glow on color complete"
```

### Task M1.7: ヒントセル pulse 演出

**Files:**
- Modify: `PixelColoringGame/Features/Game/PixelBoardView.swift`

仕様書 §3: 「ヒント塗布セルだけ pulse(光って自己主張)」。

- [ ] **Step 1: PixelBoardCellView でヒントセル判定 API を確認**

```bash
grep -n "hint\|Hint" PixelColoringGame/Features/Game/PixelBoardView.swift PixelColoringGame/Core/GameSessionStore.swift PixelColoringGame/Core/HintService.swift
```

期待: `session.isHintHighlighted(at:)` か `session.isHintCell(at:)` か等の API があるはず。

- [ ] **Step 2: pulse アニメ追加**

```swift
@State private var hintPulse: CGFloat = 0.0

private var isHinted: Bool {
    session.isHintHighlighted(at: cellIndex)  // 実 API を確認
}

// body 内、塗りレイヤーに重ねる:
.overlay {
    if isHinted, !reduceMotion {
        RoundedRectangle(cornerRadius: 3)
            .stroke(Color.white.opacity(0.8 * hintPulse), lineWidth: 2)
            .scaleEffect(1.0 + 0.15 * hintPulse)
            .animation(.easeInOut(duration: 1.0).repeatForever(autoreverses: true), value: hintPulse)
            .onAppear { hintPulse = 1.0 }
    }
}
```

Reduce Motion 時は静的 stroke のみで pulse なし。

- [ ] **Step 3: ビルド + 目視確認**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

シミュレータでヒントボタンを押してセルが光る/脈打つことを確認。

- [ ] **Step 4: コミット**

```bash
git add PixelColoringGame/Features/Game/PixelBoardView.swift
git commit -m "feat(M1): hint cell pulse animation"
```

### Task M1.8: 間違いタップ soft error tone

**Files:**
- Modify: `PixelColoringGame/Features/Game/GameView.swift`
- 既存 AudioPlayerService に短い error tone 用 API を追加 (可能なら既存音源を流用)

仕様書 §3 Layer 1: 「既存の offset shake に加え、短く柔らかい error tone」。

- [ ] **Step 1: AudioPlayerService の API を確認**

```bash
grep -rn "func play\|case .\|AudioCue" PixelColoringGame/Core/Audio/ | head -30
```

既存の cue 一覧を見て、softError 用に流用できるか判断。なければ無音で onset し M3 の QA で判断 (仕様書の「色完了音の判断プロセス」と同じ方針)。

- [ ] **Step 2: 仕様判断**

既存音源に softError 適格なものがなければ、本 Task では **追加実装を見送り**、間違いタップ時の既存 offset shake と soft impact haptic で済ませる。Phase 1 M1 完了条件にも明示。コミットは「skipped: rely on shake + soft haptic」のメモのみ。

(音源追加は new asset 必要、v1.0 範囲外と判断する基準: 仕様書 §4「新規音源は作らない」「色完了音の判断プロセス」)

- [ ] **Step 3: スキップ確認のみ、コミットなし**

このタスクはコミットなしで終了。Phase 1 M1 完了条件のチェックリストに「間違いタップ soft error tone は v0.7 では skip、shake + soft haptic で代替」と記録する。

### Task M1.9: M1 完了確認

- [ ] **Step 1: 全テストグリーン確認**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

期待: 全 test green。

- [ ] **Step 2: シミュレータで Reduce Motion ON/OFF 両方で動作確認**

設定 > アクセシビリティ > Reduce Motion を切り替えて、レベル 1 つを最初から最後までプレイする:
- Reduce Motion OFF: pop, glow, pulse すべて見える
- Reduce Motion ON: 全演出が縮退、破綻なし

- [ ] **Step 3: パフォーマンス確認 (簡易)**

Instruments での厳密計測は M2/M3 完了時に行う。M1 では `xcodebuild build` が警告なしで通れば OK。

- [ ] **Step 4: M1 完了タグ**

```bash
git tag m1-complete
git log --oneline -10
```

---

## Section M2: Layer 2 完了儀式中核 (約 3 日)

**M2 ゴール**: CelebrationEvent/Detector/Coordinator/FlowState、ParticleEmitterView、ChapterClearView、CompletionView cascade+confetti、SwiftData マイグレーション。

### Task M2.1: PlayerProfile に celebrationsSeenData 属性追加 (SwiftData マイグレーション)

**Files:**
- Modify: `PixelColoringGame/Models/` または `PixelColoringGame/Core/PlayerProfile.swift` (`@Model` クラス本体)

仕様書 §4「データモデル拡張」。

- [ ] **Step 1: PlayerProfile @Model の場所と現状を確認**

```bash
grep -rn "class PlayerProfile\b\|@Model\nclass PlayerProfile\|@Model.*PlayerProfile" PixelColoringGame/ --include="*.swift"
```

`@Model class PlayerProfile` を定義しているファイルを特定。

- [ ] **Step 2: 新規 attribute を追加**

該当ファイルの `@Model class PlayerProfile` 内に:

```swift
@Attribute var celebrationsSeenData: Data = Data()
```

- [ ] **Step 3: PlayerProfile extension に computed Set<String> を追加**

`PlayerProfileStore.swift` の既存 extension に追加 (もしくは同じファイル):

```swift
extension PlayerProfile {
    var celebrationsSeen: Set<String> {
        get {
            guard !celebrationsSeenData.isEmpty,
                  let decoded = try? JSONDecoder().decode(Set<String>.self, from: celebrationsSeenData) else {
                return []
            }
            return decoded
        }
        set {
            celebrationsSeenData = (try? JSONEncoder().encode(newValue)) ?? Data()
        }
    }
}
```

- [ ] **Step 4: ユニットテストを書く**

`PixelColoringGameTests/Celebration/CelebrationsSeenStorageTests.swift`:

```swift
import XCTest
import SwiftData
@testable import PixelColoringGame

@MainActor
final class CelebrationsSeenStorageTests: XCTestCase {
    private func makeContext() throws -> ModelContext {
        let schema = Schema([PlayerProfile.self, LevelProgress.self])
        let config = ModelConfiguration(isStoredInMemoryOnly: true)
        let container = try ModelContainer(for: schema, configurations: [config])
        return ModelContext(container)
    }

    func test_default_celebrationsSeen_isEmpty() throws {
        let ctx = try makeContext()
        let profile = PlayerProfile()
        ctx.insert(profile)
        XCTAssertEqual(profile.celebrationsSeen, [])
    }

    func test_setCelebrationsSeen_persistsRoundTrip() throws {
        let ctx = try makeContext()
        let profile = PlayerProfile()
        ctx.insert(profile)
        profile.celebrationsSeen = ["streak:7", "firstPerfect", "chapterClear:berry-meadow"]
        XCTAssertEqual(profile.celebrationsSeen, ["streak:7", "firstPerfect", "chapterClear:berry-meadow"])
    }

    func test_corruptedData_fallsBackToEmpty() throws {
        let ctx = try makeContext()
        let profile = PlayerProfile()
        ctx.insert(profile)
        profile.celebrationsSeenData = Data([0xFF, 0xAB, 0xCD])
        XCTAssertEqual(profile.celebrationsSeen, [])
    }
}
```

- [ ] **Step 5: テスト実行**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO \
  -only-testing:PixelColoringGameTests/CelebrationsSeenStorageTests 2>&1 | tail -15
```

期待: green。

- [ ] **Step 6: SwiftData マイグレーション実機検証 (M2 完了条件のうち)**

このタスクではテスト green 確認のみ。実機 M1→M2 マイグレーション検証は Task M2.13 で実施。

- [ ] **Step 7: コミット**

```bash
git add PixelColoringGame/Models/ PixelColoringGame/Core/PlayerProfileStore.swift \
        PixelColoringGameTests/Celebration/CelebrationsSeenStorageTests.swift
git commit -m "feat(M2): add celebrationsSeenData to PlayerProfile (SwiftData attribute + JSON codec)"
```

### Task M2.2: CelebrationEvent enum

**Files:**
- Create: `PixelColoringGame/Features/Celebration/CelebrationEvent.swift`

仕様書 §4「中核コンポーネント」のテーブルを実装。

- [ ] **Step 1: ファイル作成**

```swift
import Foundation

enum CelebrationEvent: Hashable, Identifiable {
    case journeyComplete
    case monthlyComplete(yearMonth: String)
    case chapterClear(chapterID: String)
    case firstPerfect
    case streakMilestone(days: Int)
    case levelCountMilestone(count: Int)

    var id: String { seenKey }

    var priority: Int {
        switch self {
        case .journeyComplete: return 100
        case .monthlyComplete: return 90
        case .chapterClear: return 80
        case .firstPerfect: return 70
        case .streakMilestone(let days):
            switch days {
            case 30: return 62
            case 14: return 61
            case 7: return 60
            default: return 60
            }
        case .levelCountMilestone(let count):
            switch count {
            case 100: return 52
            case 50: return 51
            case 10: return 50
            default: return 50
            }
        }
    }

    var seenKey: String {
        switch self {
        case .journeyComplete: return "journeyComplete"
        case .monthlyComplete(let ym): return "monthlyComplete:\(ym)"
        case .chapterClear(let id): return "chapterClear:\(id)"
        case .firstPerfect: return "firstPerfect"
        case .streakMilestone(let days): return "streak:\(days)"
        case .levelCountMilestone(let count): return "levels:\(count)"
        }
    }

    var presentationKind: CelebrationPresentationKind {
        switch self {
        case .journeyComplete, .monthlyComplete, .chapterClear: return .fullScreen
        case .firstPerfect, .streakMilestone, .levelCountMilestone: return .toast
        }
    }

    /// パターン文字列のリスト。CelebrationCoordinator がワイルドカード照合で除去する。
    var supersedesPatterns: [String] {
        switch self {
        case .journeyComplete:
            return ["chapterClear:*", "firstPerfect", "levels:*", "streak:*", "monthlyComplete:*"]
        case .monthlyComplete:
            return ["firstPerfect", "levels:*", "streak:*"]
        case .streakMilestone(let days):
            switch days {
            case 30: return ["streak:14", "streak:7"]
            case 14: return ["streak:7"]
            default: return []
            }
        case .levelCountMilestone(let count):
            switch count {
            case 100: return ["levels:50", "levels:10"]
            case 50: return ["levels:10"]
            default: return []
            }
        default:
            return []
        }
    }
}

enum CelebrationPresentationKind {
    case fullScreen
    case toast
}

extension String {
    /// シンプルなワイルドカード照合 ("streak:*" → "streak:7" にマッチ)
    func celebrationMatches(pattern: String) -> Bool {
        if pattern.hasSuffix(":*") {
            let prefix = String(pattern.dropLast()) // "streak:"
            return hasPrefix(prefix)
        }
        return self == pattern
    }
}
```

- [ ] **Step 2: ビルド**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

期待: BUILD SUCCEEDED.

- [ ] **Step 3: コミット**

```bash
git add PixelColoringGame/Features/Celebration/CelebrationEvent.swift
git commit -m "feat(M2): CelebrationEvent enum with priority/seenKey/supersedes"
```

### Task M2.3: CelebrationDetector (純関数 + TDD)

**Files:**
- Create: `PixelColoringGame/Features/Celebration/CelebrationDetector.swift`
- Create: `PixelColoringGameTests/Celebration/CelebrationDetectorTests.swift`

仕様書 §4「CelebrationDetector」。

- [ ] **Step 1: PlayerProfileSnapshot を定義 (Detector に渡す純データ)**

CelebrationDetector.swift の上部:

```swift
import Foundation

struct PlayerProfileSnapshot {
    let completedLevelCount: Int
    let perfectLevelCount: Int
    let currentStreakDays: Int
    let completedChapterIDs: Set<String>
    let isJourneyComplete: Bool
    let lastLevelChapterID: String?
    let lastLevelWasPerfect: Bool
    let lastLevelWasChapterFinalLevel: Bool
    let monthlyCompletedYearMonth: String?  // 31/31 達成した月ID、それ以外は nil
}

enum CelebrationTrigger {
    case levelCompleted(profileSnapshot: PlayerProfileSnapshot)
    case dailyCompleted(profileSnapshot: PlayerProfileSnapshot)
    case appLaunched(profileSnapshot: PlayerProfileSnapshot)
}
```

snapshot のフィールド名は仕様書を参考にし、後続 task で実コードを書く際に整合させる。

- [ ] **Step 2: テストを書く (TDD red)**

`PixelColoringGameTests/Celebration/CelebrationDetectorTests.swift`:

```swift
import XCTest
@testable import PixelColoringGame

final class CelebrationDetectorTests: XCTestCase {
    private func snapshot(
        completedLevels: Int = 0,
        perfectLevels: Int = 0,
        streak: Int = 0,
        completedChapters: Set<String> = [],
        journeyComplete: Bool = false,
        lastChapter: String? = nil,
        lastPerfect: Bool = false,
        lastWasChapterFinal: Bool = false,
        monthlyCompleted: String? = nil
    ) -> PlayerProfileSnapshot {
        PlayerProfileSnapshot(
            completedLevelCount: completedLevels,
            perfectLevelCount: perfectLevels,
            currentStreakDays: streak,
            completedChapterIDs: completedChapters,
            isJourneyComplete: journeyComplete,
            lastLevelChapterID: lastChapter,
            lastLevelWasPerfect: lastPerfect,
            lastLevelWasChapterFinalLevel: lastWasChapterFinal,
            monthlyCompletedYearMonth: monthlyCompleted
        )
    }

    func test_levelCompleted_firstPerfect_detectedWhenFirstTime() {
        let snap = snapshot(perfectLevels: 1, lastPerfect: true)
        let events = CelebrationDetector.detect(
            trigger: .levelCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertTrue(events.contains(.firstPerfect))
    }

    func test_levelCompleted_firstPerfect_notDetectedIfAlreadySeen() {
        let snap = snapshot(perfectLevels: 1, lastPerfect: true)
        let events = CelebrationDetector.detect(
            trigger: .levelCompleted(profileSnapshot: snap),
            seen: ["firstPerfect"]
        )
        XCTAssertFalse(events.contains(.firstPerfect))
    }

    func test_levelCompleted_chapterClear_detected() {
        let snap = snapshot(
            lastChapter: "berry-meadow",
            lastWasChapterFinal: true
        )
        let events = CelebrationDetector.detect(
            trigger: .levelCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertTrue(events.contains(.chapterClear(chapterID: "berry-meadow")))
    }

    func test_levelCompleted_levels10_detectedAtExactCount() {
        let snap = snapshot(completedLevels: 10)
        let events = CelebrationDetector.detect(
            trigger: .levelCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertTrue(events.contains(.levelCountMilestone(count: 10)))
    }

    func test_levelCompleted_levels100_returnsAll10_50_100() {
        let snap = snapshot(completedLevels: 100)
        let events = CelebrationDetector.detect(
            trigger: .levelCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertTrue(events.contains(.levelCountMilestone(count: 10)))
        XCTAssertTrue(events.contains(.levelCountMilestone(count: 50)))
        XCTAssertTrue(events.contains(.levelCountMilestone(count: 100)))
    }

    func test_levelCompleted_journeyComplete_detected() {
        let snap = snapshot(journeyComplete: true)
        let events = CelebrationDetector.detect(
            trigger: .levelCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertTrue(events.contains(.journeyComplete))
    }

    func test_dailyCompleted_streak7_detectedAtSeven() {
        let snap = snapshot(streak: 7)
        let events = CelebrationDetector.detect(
            trigger: .dailyCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertTrue(events.contains(.streakMilestone(days: 7)))
    }

    func test_dailyCompleted_streak6_doesNotEmit() {
        let snap = snapshot(streak: 6)
        let events = CelebrationDetector.detect(
            trigger: .dailyCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertFalse(events.contains(.streakMilestone(days: 7)))
    }

    func test_dailyCompleted_monthlyComplete_detected() {
        let snap = snapshot(monthlyCompleted: "2026-04")
        let events = CelebrationDetector.detect(
            trigger: .dailyCompleted(profileSnapshot: snap),
            seen: []
        )
        XCTAssertTrue(events.contains(.monthlyComplete(yearMonth: "2026-04")))
    }

    func test_appLaunched_returnsOnlyStreakMilestones() {
        // 同時に streak と firstPerfect 候補が成立する状態
        let snap = snapshot(
            perfectLevels: 1,
            streak: 7,
            lastPerfect: true
        )
        let events = CelebrationDetector.detect(
            trigger: .appLaunched(profileSnapshot: snap),
            seen: []
        )
        XCTAssertFalse(events.contains(.firstPerfect))
        XCTAssertTrue(events.contains(.streakMilestone(days: 7)))
    }

    func test_idempotent_sameInput_sameOutput() {
        let snap = snapshot(completedLevels: 50, streak: 14)
        let a = CelebrationDetector.detect(trigger: .dailyCompleted(profileSnapshot: snap), seen: [])
        let b = CelebrationDetector.detect(trigger: .dailyCompleted(profileSnapshot: snap), seen: [])
        XCTAssertEqual(Set(a), Set(b))
    }
}
```

- [ ] **Step 3: テスト失敗確認**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO \
  -only-testing:PixelColoringGameTests/CelebrationDetectorTests 2>&1 | tail -15
```

期待: `CelebrationDetector` 未定義でコンパイルエラー。

- [ ] **Step 4: Detector 実装**

CelebrationDetector.swift に追加:

```swift
enum CelebrationDetector {
    static func detect(
        trigger: CelebrationTrigger,
        seen: Set<String>
    ) -> [CelebrationEvent] {
        switch trigger {
        case .levelCompleted(let s):
            return detectLevelCompleted(snapshot: s, seen: seen)
        case .dailyCompleted(let s):
            return detectDailyCompleted(snapshot: s, seen: seen)
        case .appLaunched(let s):
            return detectAppLaunched(snapshot: s, seen: seen)
        }
    }

    private static func detectLevelCompleted(
        snapshot s: PlayerProfileSnapshot,
        seen: Set<String>
    ) -> [CelebrationEvent] {
        var out: [CelebrationEvent] = []

        if s.isJourneyComplete, !seen.contains("journeyComplete") {
            out.append(.journeyComplete)
        }

        if s.lastLevelWasChapterFinalLevel, let chapterID = s.lastLevelChapterID {
            let key = "chapterClear:\(chapterID)"
            if !seen.contains(key) {
                out.append(.chapterClear(chapterID: chapterID))
            }
        }

        if s.lastLevelWasPerfect, !seen.contains("firstPerfect") {
            out.append(.firstPerfect)
        }

        for threshold in [10, 50, 100] where s.completedLevelCount >= threshold {
            let key = "levels:\(threshold)"
            if !seen.contains(key) {
                out.append(.levelCountMilestone(count: threshold))
            }
        }

        return out
    }

    private static func detectDailyCompleted(
        snapshot s: PlayerProfileSnapshot,
        seen: Set<String>
    ) -> [CelebrationEvent] {
        var out: [CelebrationEvent] = []

        if let ym = s.monthlyCompletedYearMonth {
            let key = "monthlyComplete:\(ym)"
            if !seen.contains(key) {
                out.append(.monthlyComplete(yearMonth: ym))
            }
        }

        for threshold in [7, 14, 30] where s.currentStreakDays >= threshold {
            let key = "streak:\(threshold)"
            if !seen.contains(key) {
                out.append(.streakMilestone(days: threshold))
            }
        }

        if s.lastLevelWasPerfect, !seen.contains("firstPerfect") {
            out.append(.firstPerfect)
        }

        for threshold in [10, 50, 100] where s.completedLevelCount >= threshold {
            let key = "levels:\(threshold)"
            if !seen.contains(key) {
                out.append(.levelCountMilestone(count: threshold))
            }
        }

        return out
    }

    private static func detectAppLaunched(
        snapshot s: PlayerProfileSnapshot,
        seen: Set<String>
    ) -> [CelebrationEvent] {
        // 仕様書: appLaunched は streak のみ検知
        var out: [CelebrationEvent] = []
        for threshold in [7, 14, 30] where s.currentStreakDays >= threshold {
            let key = "streak:\(threshold)"
            if !seen.contains(key) {
                out.append(.streakMilestone(days: threshold))
            }
        }
        return out
    }
}
```

- [ ] **Step 5: テストグリーン確認**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO \
  -only-testing:PixelColoringGameTests/CelebrationDetectorTests 2>&1 | tail -15
```

期待: 全テスト pass。

- [ ] **Step 6: コミット**

```bash
git add PixelColoringGame/Features/Celebration/CelebrationDetector.swift \
        PixelColoringGameTests/Celebration/CelebrationDetectorTests.swift
git commit -m "feat(M2): CelebrationDetector pure function with TDD coverage"
```

### Task M2.4: CelebrationCoordinator (ObservableObject + TDD)

**Files:**
- Create: `PixelColoringGame/Features/Celebration/CelebrationCoordinator.swift`
- Create: `PixelColoringGameTests/Celebration/CelebrationCoordinatorTests.swift`

仕様書 §4「CelebrationCoordinator」。

- [ ] **Step 1: テストを書く**

`CelebrationCoordinatorTests.swift`:

```swift
import XCTest
import Combine
@testable import PixelColoringGame

@MainActor
final class CelebrationCoordinatorTests: XCTestCase {
    func test_enqueue_singleFullScreen_setsCurrent() {
        var seenStorage: Set<String> = []
        let coordinator = CelebrationCoordinator(
            initialSeen: seenStorage,
            persist: { seenStorage = $0 }
        )
        coordinator.enqueue([.chapterClear(chapterID: "berry-meadow")])
        XCTAssertEqual(coordinator.currentFullScreenEvent, .chapterClear(chapterID: "berry-meadow"))
        XCTAssertNil(coordinator.currentToast)
        XCTAssertTrue(seenStorage.contains("chapterClear:berry-meadow"))
    }

    func test_enqueue_journeyComplete_supersedesAll() {
        var seen: Set<String> = []
        let coordinator = CelebrationCoordinator(initialSeen: seen, persist: { seen = $0 })
        coordinator.enqueue([
            .firstPerfect,
            .levelCountMilestone(count: 100),
            .chapterClear(chapterID: "berry-meadow"),
            .journeyComplete,
            .streakMilestone(days: 30)
        ])
        XCTAssertEqual(coordinator.currentFullScreenEvent, .journeyComplete)
        // 全イベント seen に保存される
        XCTAssertEqual(seen, [
            "journeyComplete",
            "chapterClear:berry-meadow",
            "firstPerfect",
            "levels:100",
            "streak:30"
        ])
    }

    func test_enqueue_streak30_supersedesStreak7And14() {
        var seen: Set<String> = []
        let coordinator = CelebrationCoordinator(initialSeen: seen, persist: { seen = $0 })
        coordinator.enqueue([
            .streakMilestone(days: 7),
            .streakMilestone(days: 14),
            .streakMilestone(days: 30)
        ])
        XCTAssertEqual(coordinator.currentToast, .streakMilestone(days: 30))
        XCTAssertEqual(seen, ["streak:7", "streak:14", "streak:30"])
    }

    func test_duplicateEnqueue_doesNotDouble() {
        var seen: Set<String> = ["firstPerfect"]
        let coordinator = CelebrationCoordinator(initialSeen: seen, persist: { seen = $0 })
        coordinator.enqueue([.firstPerfect])
        XCTAssertNil(coordinator.currentToast)
        XCTAssertNil(coordinator.currentFullScreenEvent)
    }

    func test_dismissCurrent_advancesQueue() {
        var seen: Set<String> = []
        let coordinator = CelebrationCoordinator(initialSeen: seen, persist: { seen = $0 })
        coordinator.enqueue([
            .chapterClear(chapterID: "a"),
            .chapterClear(chapterID: "b")
        ])
        XCTAssertEqual(coordinator.currentFullScreenEvent, .chapterClear(chapterID: "a"))
        coordinator.dismissCurrent()
        XCTAssertEqual(coordinator.currentFullScreenEvent, .chapterClear(chapterID: "b"))
        coordinator.dismissCurrent()
        XCTAssertNil(coordinator.currentFullScreenEvent)
    }

    func test_completionFlowActive_whenAnyEvent() {
        var seen: Set<String> = []
        let coordinator = CelebrationCoordinator(initialSeen: seen, persist: { seen = $0 })
        XCTAssertFalse(coordinator.isCompletionFlowActive)
        coordinator.enqueue([.chapterClear(chapterID: "a")])
        XCTAssertTrue(coordinator.isCompletionFlowActive)
        coordinator.dismissCurrent()
        XCTAssertFalse(coordinator.isCompletionFlowActive)
    }
}
```

- [ ] **Step 2: Coordinator 実装**

`CelebrationCoordinator.swift`:

```swift
import Foundation
import Combine

@MainActor
final class CelebrationCoordinator: ObservableObject {
    @Published private(set) var currentFullScreenEvent: CelebrationEvent?
    @Published private(set) var currentToast: CelebrationEvent?

    private var seen: Set<String>
    private var fullScreenQueue: [CelebrationEvent] = []
    private var toastQueue: [CelebrationEvent] = []
    private let persistSeen: (Set<String>) -> Void

    init(initialSeen: Set<String>, persist: @escaping (Set<String>) -> Void) {
        self.seen = initialSeen
        self.persistSeen = persist
    }

    var isCompletionFlowActive: Bool {
        currentFullScreenEvent != nil
            || currentToast != nil
            || !fullScreenQueue.isEmpty
            || !toastQueue.isEmpty
    }

    func handle(trigger: CelebrationTrigger) {
        let events = CelebrationDetector.detect(trigger: trigger, seen: seen)
        enqueue(events)
    }

    func enqueue(_ events: [CelebrationEvent]) {
        let pruned = applySupersedes(events: events)
        for ev in pruned {
            guard !seen.contains(ev.seenKey) else { continue }
            switch ev.presentationKind {
            case .fullScreen:
                fullScreenQueue.append(ev)
            case .toast:
                toastQueue.append(ev)
            }
        }
        fullScreenQueue.sort { $0.priority > $1.priority }
        toastQueue.sort { $0.priority > $1.priority }
        advance()
    }

    func dismissCurrent() {
        currentFullScreenEvent = nil
        currentToast = nil
        advance()
    }

    private func advance() {
        if currentFullScreenEvent == nil, let next = fullScreenQueue.first {
            fullScreenQueue.removeFirst()
            markSeen(next)
            currentFullScreenEvent = next
        }
        if currentToast == nil, let next = toastQueue.first {
            toastQueue.removeFirst()
            markSeen(next)
            currentToast = next
        }
    }

    private func markSeen(_ event: CelebrationEvent) {
        seen.insert(event.seenKey)
        // supersedes 対象 (Event 内で吸収されるはずだったマイルストーン) も seen に保存
        for pattern in event.supersedesPatterns {
            // パターンが具体的なキー (例: "firstPerfect") の場合のみ追加
            if !pattern.hasSuffix(":*") {
                seen.insert(pattern)
            }
            // ワイルドカードはここで保存対象を確定できないため
            // 同 enqueue 内で除去されたイベントは applySupersedes 内で seen 追加する
        }
        persistSeen(seen)
    }

    /// supersedes ルールに従って、優先度低いイベントを除去。
    /// 除去されたイベントは seen に保存する (二度と発火させない契約)。
    private func applySupersedes(events: [CelebrationEvent]) -> [CelebrationEvent] {
        let sorted = events.sorted { $0.priority > $1.priority }
        var kept: [CelebrationEvent] = []
        for ev in sorted {
            let isSuperseded = kept.contains { higher in
                higher.supersedesPatterns.contains { pattern in
                    ev.seenKey.celebrationMatches(pattern: pattern)
                }
            }
            if isSuperseded {
                seen.insert(ev.seenKey)
            } else {
                kept.append(ev)
            }
        }
        if !seen.isEmpty { persistSeen(seen) }
        return kept
    }
}
```

- [ ] **Step 3: テストグリーン確認**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO \
  -only-testing:PixelColoringGameTests/CelebrationCoordinatorTests 2>&1 | tail -15
```

期待: 全 pass.

- [ ] **Step 4: コミット**

```bash
git add PixelColoringGame/Features/Celebration/CelebrationCoordinator.swift \
        PixelColoringGameTests/Celebration/CelebrationCoordinatorTests.swift
git commit -m "feat(M2): CelebrationCoordinator with priority queue and supersedes"
```

### Task M2.5: ParticleEmitterView (.confetti)

**Files:**
- Create: `PixelColoringGame/Features/Celebration/ParticleEmitterView.swift`

仕様書 §4「ParticleEmitterView」。.confetti のみ実装。

- [ ] **Step 1: ファイル作成**

```swift
import SwiftUI

struct ParticleEmitterView: View {
    enum Preset { case confetti }

    let preset: Preset
    let profile: AnimationProfile
    @State private var startTime: Date = .now

    private struct Particle: Identifiable {
        let id: Int
        let color: Color
        let startX: CGFloat
        let drift: CGFloat
        let spin: Double
        let size: CGFloat
        let duration: Double
        let delay: Double
    }

    private var particles: [Particle] {
        let count = profile.particleBudget
        guard count > 0 else { return [] }
        let palette: [Color] = [
            Color(red: 1.0, green: 0.78, blue: 0.42),
            Color(red: 0.96, green: 0.55, blue: 0.78),
            Color(red: 0.60, green: 0.78, blue: 0.96),
            Color(red: 0.70, green: 0.92, blue: 0.66),
            Color(red: 0.95, green: 0.93, blue: 0.55),
        ]
        var generator = SystemRandomNumberGenerator()
        return (0..<count).map { i in
            Particle(
                id: i,
                color: palette[i % palette.count],
                startX: CGFloat.random(in: 0...1, using: &generator),
                drift: CGFloat.random(in: -0.2...0.2, using: &generator),
                spin: Double.random(in: 0.5...3.5, using: &generator),
                size: CGFloat.random(in: 6...12, using: &generator),
                duration: Double.random(in: 1.8...3.0, using: &generator),
                delay: Double.random(in: 0...0.6, using: &generator)
            )
        }
    }

    var body: some View {
        guard profile.shouldShowParticles else {
            return AnyView(EmptyView())
        }
        let snapshot = particles
        return AnyView(
            TimelineView(.animation) { context in
                Canvas { ctx, size in
                    let elapsed = context.date.timeIntervalSince(startTime)
                    for p in snapshot {
                        let local = elapsed - p.delay
                        guard local >= 0, local <= p.duration else { continue }
                        let progress = local / p.duration
                        let y = size.height * CGFloat(progress)
                        let x = size.width * (p.startX + p.drift * CGFloat(progress))
                        let rotation = Angle.degrees(p.spin * 360.0 * progress)
                        let rect = CGRect(
                            x: x - p.size / 2,
                            y: y - p.size / 2,
                            width: p.size,
                            height: p.size * 0.6
                        )
                        var transform = CGAffineTransform.identity
                            .translatedBy(x: rect.midX, y: rect.midY)
                            .rotated(by: rotation.radians)
                            .translatedBy(x: -rect.midX, y: -rect.midY)
                        let path = Path(rect).applying(transform)
                        ctx.fill(path, with: .color(p.color.opacity(1.0 - progress * 0.4)))
                    }
                }
            }
            .allowsHitTesting(false)
        )
    }
}

#Preview {
    ParticleEmitterView(
        preset: .confetti,
        profile: AnimationProfile(reduceMotion: false, lowPower: false)
    )
    .frame(width: 320, height: 480)
    .background(Color.black)
}
```

- [ ] **Step 2: ビルド + プレビュー目視**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

Xcode で Preview を見て confetti が降ってくることを確認 (人手)。

- [ ] **Step 3: コミット**

```bash
git add PixelColoringGame/Features/Celebration/ParticleEmitterView.swift
git commit -m "feat(M2): ParticleEmitterView with .confetti preset (Canvas + TimelineView)"
```

### Task M2.6: ChapterClearView

**Files:**
- Create: `PixelColoringGame/Features/Celebration/ChapterClearView.swift`

仕様書 §3 Layer 2 / §4 + 既存 ChapterDefinition / JourneyRepository を参照。

- [ ] **Step 1: 既存 Chapter API を確認**

```bash
grep -rn "ChapterDefinition\|class.*Chapter\|chapterTitle" PixelColoringGame/ --include="*.swift" | head -20
```

- [ ] **Step 2: ChapterClearView 実装**

```swift
import SwiftUI

struct ChapterClearView: View {
    let chapterID: String
    let chapterTitleKey: String
    let nextChapterTitleKey: String?
    let onDismiss: () -> Void

    @Environment(\.accessibilityReduceMotion) private var reduceMotion
    @State private var badgeScale: CGFloat = 0.6
    @State private var badgeOpacity: Double = 0.0

    private var profile: AnimationProfile {
        AnimationProfile(
            reduceMotion: reduceMotion,
            lowPower: ProcessInfo.processInfo.isLowPowerModeEnabled
        )
    }

    var body: some View {
        ZStack {
            AppTheme.completionBackground.ignoresSafeArea()

            VStack(spacing: 32) {
                Spacer(minLength: 0)
                badge
                titleBlock
                nextChapterHint
                Spacer(minLength: 0)
                continueButton
            }
            .padding(.horizontal, 32)
            .padding(.vertical, 48)

            ParticleEmitterView(preset: .confetti, profile: profile)
                .ignoresSafeArea()
        }
        .onAppear {
            withAnimation(profile.pop().delay(0.1)) {
                badgeScale = 1.0
                badgeOpacity = 1.0
            }
        }
    }

    private var badge: some View {
        Circle()
            .fill(AppTheme.celebrationGold)
            .frame(width: 160, height: 160)
            .overlay(
                Image(systemName: "checkmark.seal.fill")
                    .resizable()
                    .scaledToFit()
                    .padding(36)
                    .foregroundStyle(.white)
            )
            .shadow(radius: 24)
            .scaleEffect(badgeScale)
            .opacity(badgeOpacity)
    }

    private var titleBlock: some View {
        VStack(spacing: 8) {
            Text("chapter.clear.title".localized)
                .font(.system(size: 18, weight: .bold, design: .rounded))
                .foregroundStyle(AppTheme.textSecondary)
            Text(chapterTitleKey.localized)
                .font(.system(size: 32, weight: .black, design: .rounded))
                .foregroundStyle(AppTheme.textPrimary)
        }
        .multilineTextAlignment(.center)
    }

    @ViewBuilder
    private var nextChapterHint: some View {
        if let key = nextChapterTitleKey {
            VStack(spacing: 6) {
                Text("chapter.clear.next".localized)
                    .font(.system(size: 14, weight: .semibold, design: .rounded))
                    .foregroundStyle(AppTheme.textTertiary)
                Text(key.localized)
                    .font(.system(size: 18, weight: .bold, design: .rounded))
                    .foregroundStyle(AppTheme.textPrimary)
            }
        }
    }

    private var continueButton: some View {
        Button(action: onDismiss) {
            Text("chapter.clear.continue".localized)
                .font(.system(size: 18, weight: .bold, design: .rounded))
                .foregroundStyle(.white)
                .frame(maxWidth: .infinity)
                .frame(height: 56)
                .background(
                    RoundedRectangle(cornerRadius: 28, style: .continuous)
                        .fill(AppTheme.celebrationGold)
                )
        }
        .buttonStyle(.plain)
    }
}

private extension String {
    var localized: String { NSLocalizedString(self, comment: "") }
}

#if DEBUG
#Preview("ChapterClear 通常") {
    ChapterClearView(
        chapterID: "berry-meadow",
        chapterTitleKey: "chapter.berry-meadow.title",
        nextChapterTitleKey: "chapter.cloud-port.title",
        onDismiss: {}
    )
}

#Preview("ChapterClear Reduce Motion") {
    ChapterClearView(
        chapterID: "berry-meadow",
        chapterTitleKey: "chapter.berry-meadow.title",
        nextChapterTitleKey: nil,
        onDismiss: {}
    )
    .environment(\.accessibilityReduceMotion, true)
}
#endif
```

注: `AppTheme.completionBackground`, `AppTheme.celebrationGold`, `AppTheme.textTertiary` が既存に無い場合、AppTheme.swift に追加するか、近い既存色 (例: `AppTheme.accent`) で代用する。

- [ ] **Step 3: ローカライズキーを Localizable.xcstrings に追加**

新規キー:
- `chapter.clear.title` → "Chapter Cleared" / "章クリア!"
- `chapter.clear.next` → "Up Next" / "次の章"
- `chapter.clear.continue` → "Continue" / "つづける"

xcstrings は Xcode で開いて追加するのが王道だが、JSON 直編集も可能。本タスクでは Xcode で開くか、`PixelColoringGame/Resources/Localizable.xcstrings` を直接編集してキーを追加する。

- [ ] **Step 4: ビルド**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

- [ ] **Step 5: コミット**

```bash
git add PixelColoringGame/Features/Celebration/ChapterClearView.swift \
        PixelColoringGame/Resources/Localizable.xcstrings
git commit -m "feat(M2): ChapterClearView with confetti and badge cascade"
```

### Task M2.7: CelebrationFlowState (CompletionView 用ローカル VM)

**Files:**
- Create: `PixelColoringGame/Features/Celebration/CelebrationFlowState.swift`

仕様書 §4「CelebrationFlowState」。

- [ ] **Step 1: 実装**

```swift
import SwiftUI
import Combine

@MainActor
final class CelebrationFlowState: ObservableObject {
    enum Stage: Int, CaseIterable {
        case background = 0
        case artwork
        case rank
        case stats
        case cta
    }

    @Published var visibleStages: Set<Stage> = []
    @Published var confettiTriggered: Bool = false
    @Published var isCTAReady: Bool = false

    private let profile: AnimationProfile

    init(profile: AnimationProfile = .current) {
        self.profile = profile
    }

    func start() {
        if profile.reduceMotion {
            visibleStages = Set(Stage.allCases)
            confettiTriggered = true
            isCTAReady = true
            return
        }

        for stage in Stage.allCases {
            let delay = profile.cascadeDelay(stage.rawValue)
            DispatchQueue.main.asyncAfter(deadline: .now() + delay) { [weak self] in
                guard let self else { return }
                withAnimation(.easeOut(duration: 0.35)) {
                    self.visibleStages.insert(stage)
                }
                if stage == .artwork {
                    self.confettiTriggered = true
                }
                if stage == .cta {
                    self.isCTAReady = true
                }
            }
        }
    }
}
```

- [ ] **Step 2: ビルド**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

- [ ] **Step 3: コミット**

```bash
git add PixelColoringGame/Features/Celebration/CelebrationFlowState.swift
git commit -m "feat(M2): CelebrationFlowState for CompletionView cascade choreography"
```

### Task M2.8: CompletionView を cascade + confetti に改修

**Files:**
- Modify: `PixelColoringGame/Features/Completion/CompletionView.swift:150-299`

仕様書 §4「既存コードへの介入ポイント」+ §3 Layer 2。

- [ ] **Step 1: CompletionView 150-299 を Read**

現状の構造を理解。bg/artwork/rank/stats/CTA の各要素をどこで描画しているかを把握。

- [ ] **Step 2: CelebrationFlowState を注入し、各要素に opacity/offset 切替を追加**

各要素の周辺に:

```swift
@StateObject private var flowState = CelebrationFlowState()

// 各要素:
.opacity(flowState.visibleStages.contains(.artwork) ? 1.0 : 0.0)
.offset(y: flowState.visibleStages.contains(.artwork) ? 0 : 20)
.onAppear { flowState.start() }
```

- [ ] **Step 3: 章アンロックバナーは削除**

仕様書 §4「章アンロックバナー削除」を遵守。ただし `CompletionView.swift:269-296` の「unlocked event title」(Monthly イベント側) は残す。実コードを Read して該当箇所を特定。

- [ ] **Step 4: confetti overlay 追加**

CompletionView の最上層に:

```swift
.overlay {
    if flowState.confettiTriggered {
        ParticleEmitterView(preset: .confetti, profile: .current)
            .ignoresSafeArea()
            .allowsHitTesting(false)
    }
}
```

- [ ] **Step 5: ビルド + テスト**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

期待: 全テスト pass、ビルド成功。既存の CompletionView 関連テストがあれば期待値調整が必要。

- [ ] **Step 6: シミュレータで目視**

レベル 1 つを完了 → CompletionView の cascade + confetti を確認。Reduce Motion ON で破綻なし。

- [ ] **Step 7: コミット**

```bash
git add PixelColoringGame/Features/Completion/CompletionView.swift
git commit -m "feat(M2): CompletionView cascade entrance and confetti overlay"
```

### Task M2.9: AppView に CelebrationCoordinator を注入し fullScreenCover を配線

**Files:**
- Modify: `PixelColoringGame/App/AppView.swift`

仕様書 §4「既存コードへの介入ポイント」AppView 行。

- [ ] **Step 1: AppView を Read**

CelebrationCoordinator をどこで生成し、子 View に渡すか確認。

- [ ] **Step 2: AppView に Coordinator 生成**

`@State private var celebrationCoordinator: CelebrationCoordinator?` (PlayerProfile が必要なので、profile load 後に初期化)。

profile bootstrap 後に:

```swift
celebrationCoordinator = CelebrationCoordinator(
    initialSeen: profile.celebrationsSeen,
    persist: { newSeen in
        profile.celebrationsSeen = newSeen
        try? modelContext.save()
    }
)
```

- [ ] **Step 3: 子 View に environmentObject 注入**

`RootTabView` に `.environmentObject(celebrationCoordinator)` を追加。

- [ ] **Step 4: ルートに fullScreenCover を追加**

```swift
.fullScreenCover(item: $celebrationCoordinator.currentFullScreenEvent) { event in
    celebrationFullScreenView(for: event)
}
```

`celebrationFullScreenView(for:)` は switch 文で `ChapterClearView` / `JourneyCompleteView` / `MonthlyCompleteView` を返す。M2 では `ChapterClearView` のみ実装、他は `EmptyView()` でプレースホルダ (M3 で実装)。

- [ ] **Step 5: ビルド + 目視**

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

- [ ] **Step 6: コミット**

```bash
git add PixelColoringGame/App/AppView.swift
git commit -m "feat(M2): wire CelebrationCoordinator into AppView root"
```

### Task M2.10: GameSessionStore.finishLevel から coordinator.handle 呼び出し

**Files:**
- Modify: `PixelColoringGame/Core/GameSessionStore.swift`

仕様書 §4: `GameSessionStore.finishLevel` 末尾で `coordinator.handle(trigger: .levelCompleted(...))`。

- [ ] **Step 1: GameSessionStore.finishLevel を Read**

完了時に PlayerProfile / completed counts へどのようにアクセスできるか確認。Snapshot ビルダーが必要。

- [ ] **Step 2: PlayerProfileSnapshot のビルダーを追加**

`PlayerProfileStore` に:

```swift
func makeSnapshot(
    profile: PlayerProfile,
    lastLevelChapterID: String?,
    lastLevelWasPerfect: Bool,
    lastLevelWasChapterFinalLevel: Bool,
    isJourneyComplete: Bool,
    monthlyCompletedYearMonth: String?
) -> PlayerProfileSnapshot {
    PlayerProfileSnapshot(
        completedLevelCount: profile.completedLevelIDs.count,  // 実 API 名は確認
        perfectLevelCount: profile.perfectLevelIDs.count,
        currentStreakDays: profile.currentStreak,
        completedChapterIDs: Set(profile.completedChapterIDs),
        isJourneyComplete: isJourneyComplete,
        lastLevelChapterID: lastLevelChapterID,
        lastLevelWasPerfect: lastLevelWasPerfect,
        lastLevelWasChapterFinalLevel: lastLevelWasChapterFinalLevel,
        monthlyCompletedYearMonth: monthlyCompletedYearMonth
    )
}
```

実フィールド名 (`completedLevelIDs` / `perfectLevelIDs`) は PlayerProfile 既存定義から確認して整合。なければ ProgressStore から count を取る。

- [ ] **Step 3: GameSessionStore.finishLevel 末尾で trigger 発火**

```swift
let snapshot = playerProfileStore.makeSnapshot(profile: profile, ...)
coordinator.handle(trigger: .levelCompleted(profileSnapshot: snapshot))
```

GameSessionStore に coordinator 参照を持たせる必要あり: イニシャライザに追加、AppView から渡す。

- [ ] **Step 4: 既存テスト調整**

`GameSessionStoreTests.swift` でイニシャライザが変わるので、テスト側に dummy coordinator を渡すヘルパーを追加。

- [ ] **Step 5: ビルド + テスト**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

- [ ] **Step 6: コミット**

```bash
git add PixelColoringGame/Core/GameSessionStore.swift \
        PixelColoringGame/Core/PlayerProfileStore.swift \
        PixelColoringGameTests/GameSessionStoreTests.swift
git commit -m "feat(M2): GameSessionStore.finishLevel fires celebration trigger"
```

### Task M2.11: DebugMenu 最小版 (Title 長押し起動)

**Files:**
- Create: `PixelColoringGame/Features/Debug/DebugMenuView.swift`
- Modify: `PixelColoringGame/Features/Title/TitleScreenView.swift`

仕様書 §4「DebugMenu」+「TitleScreenView」介入ポイント。

- [ ] **Step 1: DebugMenuView 作成 (#if DEBUG)**

```swift
#if DEBUG
import SwiftUI
import SwiftData

struct DebugMenuView: View {
    @Environment(\.modelContext) private var modelContext
    @EnvironmentObject var coordinator: CelebrationCoordinator
    @Query private var profiles: [PlayerProfile]

    var body: some View {
        NavigationStack {
            List {
                Section("Celebrations Seen") {
                    Button("Clear all celebrationsSeen") {
                        guard let profile = profiles.first else { return }
                        profile.celebrationsSeen = []
                        try? modelContext.save()
                    }
                    ForEach(seenList, id: \.self) { key in
                        HStack {
                            Text(key)
                            Spacer()
                            Button("Clear") { clearSeen(key) }
                        }
                    }
                }

                Section("Fire celebration") {
                    Button("Chapter Clear (berry-meadow)") {
                        coordinator.handle(trigger: .levelCompleted(
                            profileSnapshot: testSnapshot(chapterFinal: "berry-meadow"))
                        )
                    }
                    Button("First Perfect") {
                        coordinator.handle(trigger: .levelCompleted(
                            profileSnapshot: testSnapshot(perfect: true))
                        )
                    }
                    Button("Levels 100") {
                        coordinator.handle(trigger: .levelCompleted(
                            profileSnapshot: testSnapshot(completedLevels: 100))
                        )
                    }
                }
            }
            .navigationTitle("Debug Menu")
        }
    }

    private var seenList: [String] {
        Array((profiles.first?.celebrationsSeen ?? []).sorted())
    }

    private func clearSeen(_ key: String) {
        guard let profile = profiles.first else { return }
        var seen = profile.celebrationsSeen
        seen.remove(key)
        profile.celebrationsSeen = seen
        try? modelContext.save()
    }

    private func testSnapshot(
        completedLevels: Int = 0,
        perfect: Bool = false,
        chapterFinal: String? = nil
    ) -> PlayerProfileSnapshot {
        PlayerProfileSnapshot(
            completedLevelCount: completedLevels,
            perfectLevelCount: perfect ? 1 : 0,
            currentStreakDays: 0,
            completedChapterIDs: [],
            isJourneyComplete: false,
            lastLevelChapterID: chapterFinal,
            lastLevelWasPerfect: perfect,
            lastLevelWasChapterFinalLevel: chapterFinal != nil,
            monthlyCompletedYearMonth: nil
        )
    }
}
#endif
```

- [ ] **Step 2: TitleScreenView に長押し追加**

```swift
#if DEBUG
@State private var showDebugMenu = false

// バージョンラベルに:
.onLongPressGesture(minimumDuration: 2.0) {
    UINotificationFeedbackGenerator().notificationOccurred(.success)
    showDebugMenu = true
}
.sheet(isPresented: $showDebugMenu) {
    DebugMenuView()
}
#endif
```

- [ ] **Step 3: ビルド**

Release ビルドでも DebugMenu が除外されることを確認:

```bash
xcodebuild build -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' \
  -configuration Release \
  -derivedDataPath /tmp/PixelColoringGameDerivedRelease CODE_SIGNING_ALLOWED=NO 2>&1 | tail -10
```

期待: BUILD SUCCEEDED.

- [ ] **Step 4: コミット**

```bash
git add PixelColoringGame/Features/Debug/DebugMenuView.swift \
        PixelColoringGame/Features/Title/TitleScreenView.swift
git commit -m "feat(M2): DebugMenu (DEBUG only) for celebration manual firing"
```

### Task M2.12: M2 完了確認 + SwiftData マイグレーション実機検証

- [ ] **Step 1: 全テスト green**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

- [ ] **Step 2: M1→M2 マイグレーション検証 (実機シミュ)**

仕様書 M2 完了条件:

1. M1 tag に巻き戻したビルドでテストプロファイル作成 (数レベルクリア + streak 数日 + 章クリア)
2. M2 build にアップグレード
3. 既存進捗保持 + celebrationsSeenData=空 Set 確認

```bash
# M1 ビルド
git checkout m1-complete -- .
xcodebuild build -scheme PixelColoringGame ... # シミュレータインストール
# 操作: レベル数個クリア、デイリーストリーク数日継続
git checkout main -- .
xcodebuild build -scheme PixelColoringGame ... # 再インストール (アンインストールしない)
# 確認: 進捗が保持されている、celebrationsSeen が空
```

- [ ] **Step 3: ChapterClearView 実機確認**

DebugMenu から「Chapter Clear (berry-meadow)」を発火 → ChapterClearView が表示されることを確認。

- [ ] **Step 4: M2 tag**

```bash
git tag m2-complete
git log --oneline -10
```

---

## Section M3: Layer 2 拡張 + Layer 3 (約 3 日)

**M3 ゴール**: JourneyCompleteView / MonthlyCompleteView、MilestoneToastHost、Daily 統合、appLaunched 検知、DebugMenu 拡張。

### Task M3.1: JourneyCompleteView

**Files:**
- Create: `PixelColoringGame/Features/Celebration/JourneyCompleteView.swift`

仕様書 §3 Layer 2「Journey完走画面」。

- [ ] **Step 1: ChapterClearView をベースに JourneyCompleteView を作る**

修了証的デザイン: 大型バッジ + チェックマーク + Journey 完走テキスト。ChapterClearView と同じ confetti + cascade パターン。

ChapterClearView の構造をコピーして、テキスト/色味/バッジサイズを調整。

```swift
import SwiftUI

struct JourneyCompleteView: View {
    let onDismiss: () -> Void

    @Environment(\.accessibilityReduceMotion) private var reduceMotion
    @State private var medalScale: CGFloat = 0.5

    private var profile: AnimationProfile { .current }

    var body: some View {
        ZStack {
            AppTheme.celebrationDeep.ignoresSafeArea()
            VStack(spacing: 32) {
                Spacer()
                medal
                title
                Spacer()
                continueButton
            }
            .padding(.horizontal, 32)
            .padding(.vertical, 48)

            ParticleEmitterView(preset: .confetti, profile: profile)
                .ignoresSafeArea()
        }
        .onAppear {
            withAnimation(profile.pop().delay(0.15)) { medalScale = 1.0 }
        }
    }

    private var medal: some View {
        Image(systemName: "star.circle.fill")
            .resizable().scaledToFit()
            .frame(width: 200, height: 200)
            .foregroundStyle(AppTheme.celebrationGold)
            .scaleEffect(medalScale)
            .shadow(radius: 30)
    }

    private var title: some View {
        VStack(spacing: 12) {
            Text("journey.complete.eyebrow".localized)
                .font(.system(size: 18, weight: .bold, design: .rounded))
                .foregroundStyle(.white.opacity(0.7))
            Text("journey.complete.title".localized)
                .font(.system(size: 36, weight: .black, design: .rounded))
                .foregroundStyle(.white)
            Text("journey.complete.subtitle".localized)
                .font(.system(size: 16, weight: .medium, design: .rounded))
                .foregroundStyle(.white.opacity(0.8))
                .multilineTextAlignment(.center)
        }
    }

    private var continueButton: some View {
        Button(action: onDismiss) {
            Text("journey.complete.continue".localized)
                .font(.system(size: 18, weight: .bold, design: .rounded))
                .foregroundStyle(AppTheme.celebrationDeep)
                .frame(maxWidth: .infinity, minHeight: 56)
                .background(RoundedRectangle(cornerRadius: 28).fill(AppTheme.celebrationGold))
        }
        .buttonStyle(.plain)
    }
}

private extension String {
    var localized: String { NSLocalizedString(self, comment: "") }
}

#if DEBUG
#Preview { JourneyCompleteView(onDismiss: {}) }
#endif
```

- [ ] **Step 2: ローカライズキーを追加**

`journey.complete.eyebrow`: "Journey Complete" / "旅の完走"
`journey.complete.title`: "You Bloomed Every Pixel" / "全てのピクセルが咲きました"
`journey.complete.subtitle`: "Thank you for coloring with us." / "あなたの旅をありがとう"
`journey.complete.continue`: "Return Home" / "ホームに戻る"

- [ ] **Step 3: AppView の celebrationFullScreenView(for:) で JourneyCompleteView をマップ**

- [ ] **Step 4: ビルド + DebugMenu 発火確認**

DebugMenu に「Journey Complete」ボタンを追加し、発火させて目視確認。

- [ ] **Step 5: コミット**

```bash
git add PixelColoringGame/Features/Celebration/JourneyCompleteView.swift \
        PixelColoringGame/Features/Debug/DebugMenuView.swift \
        PixelColoringGame/App/AppView.swift \
        PixelColoringGame/Resources/Localizable.xcstrings
git commit -m "feat(M3): JourneyCompleteView with medal cascade"
```

### Task M3.2: MonthlyCompleteView (CTA 動的切替)

**Files:**
- Create: `PixelColoringGame/Features/Celebration/MonthlyCompleteView.swift`

仕様書 §5 シナリオ4 + 「MonthlyCompleteView CTA」。

- [ ] **Step 1: CatalogService.isReleased(nextYearMonth) を確認**

```bash
grep -rn "isReleased\|CatalogService" PixelColoringGame/ --include="*.swift" | head -10
```

無ければ、`DailyChallengeRepository` か `JourneyRepository` の API で次月コンテンツ有無を判定する。代替: `dailyRepository.hasContent(forYearMonth:)` 等。

- [ ] **Step 2: MonthlyCompleteView 実装**

```swift
import SwiftUI

struct MonthlyCompleteView: View {
    let yearMonth: String
    let hasNextMonth: Bool
    let onContinue: () -> Void
    let onGoToNextMonth: () -> Void

    @Environment(\.accessibilityReduceMotion) private var reduceMotion
    @State private var calendarScale: CGFloat = 0.7

    private var profile: AnimationProfile { .current }

    var body: some View {
        ZStack {
            AppTheme.celebrationMonthly.ignoresSafeArea()
            VStack(spacing: 28) {
                Spacer()
                badge
                titleBlock
                Spacer()
                cta
            }
            .padding(.horizontal, 32).padding(.vertical, 48)

            ParticleEmitterView(preset: .confetti, profile: profile)
                .ignoresSafeArea()
        }
        .onAppear {
            withAnimation(profile.pop().delay(0.1)) { calendarScale = 1.0 }
        }
    }

    private var badge: some View {
        Image(systemName: "calendar.badge.checkmark")
            .resizable().scaledToFit()
            .frame(width: 180, height: 180)
            .foregroundStyle(.white)
            .scaleEffect(calendarScale)
            .shadow(radius: 20)
    }

    private var titleBlock: some View {
        VStack(spacing: 10) {
            Text("monthly.complete.eyebrow".localized)
                .font(.system(size: 16, weight: .bold, design: .rounded))
                .foregroundStyle(.white.opacity(0.7))
            Text(yearMonth)
                .font(.system(size: 28, weight: .black, design: .rounded))
                .foregroundStyle(.white)
            Text("monthly.complete.subtitle".localized)
                .font(.system(size: 16, weight: .medium, design: .rounded))
                .foregroundStyle(.white.opacity(0.85))
                .multilineTextAlignment(.center)
        }
    }

    @ViewBuilder
    private var cta: some View {
        if hasNextMonth {
            primaryButton(title: "monthly.complete.nextMonth".localized, action: onGoToNextMonth)
        } else {
            primaryButton(title: "monthly.complete.continue".localized, action: onContinue)
        }
    }

    private func primaryButton(title: String, action: @escaping () -> Void) -> some View {
        Button(action: action) {
            Text(title)
                .font(.system(size: 18, weight: .bold, design: .rounded))
                .foregroundStyle(AppTheme.celebrationMonthly)
                .frame(maxWidth: .infinity, minHeight: 56)
                .background(RoundedRectangle(cornerRadius: 28).fill(.white))
        }
        .buttonStyle(.plain)
    }
}

private extension String {
    var localized: String { NSLocalizedString(self, comment: "") }
}
```

- [ ] **Step 3: ローカライズキー追加**

`monthly.complete.eyebrow`: "Monthly Complete" / "今月コンプリート"
`monthly.complete.subtitle`: "31/31 days bloomed." / "31/31 日、花咲きました"
`monthly.complete.nextMonth`: "Next Month →" / "次の月へ →"
`monthly.complete.continue`: "Continue" / "つづける"

- [ ] **Step 4: AppView でマップ**

`celebrationFullScreenView(for:)` の `.monthlyComplete(let ym)` ケースで `MonthlyCompleteView` を返し、`hasNextMonth` を `dailyRepository.hasContent(forYearMonth: nextMonthFromYM(ym))` で算出。

- [ ] **Step 5: ビルド + DebugMenu 発火確認**

- [ ] **Step 6: コミット**

```bash
git add PixelColoringGame/Features/Celebration/MonthlyCompleteView.swift \
        PixelColoringGame/App/AppView.swift \
        PixelColoringGame/Features/Debug/DebugMenuView.swift \
        PixelColoringGame/Resources/Localizable.xcstrings
git commit -m "feat(M3): MonthlyCompleteView with dynamic CTA"
```

### Task M3.3: MilestoneToastView + MilestoneToastHost

**Files:**
- Create: `PixelColoringGame/Features/Celebration/MilestoneToastView.swift`
- Create: `PixelColoringGame/Features/Celebration/MilestoneToastHost.swift`

仕様書 §3 Layer 3 + §5「トースト連続提示ルール」。

- [ ] **Step 1: MilestoneToastView 実装 (単一トースト)**

```swift
import SwiftUI

struct MilestoneToastView: View {
    let event: CelebrationEvent
    let onTap: () -> Void

    var body: some View {
        HStack(spacing: 14) {
            Image(systemName: icon).foregroundStyle(.yellow)
            VStack(alignment: .leading, spacing: 4) {
                Text(titleKey.localized).font(.headline)
                Text(subtitleKey.localized).font(.subheadline).foregroundStyle(.secondary)
            }
            Spacer()
        }
        .padding(.horizontal, 18).padding(.vertical, 14)
        .background(
            RoundedRectangle(cornerRadius: 18, style: .continuous)
                .fill(.ultraThinMaterial)
        )
        .onTapGesture(perform: onTap)
        .padding(.horizontal, 20)
    }

    private var icon: String {
        switch event {
        case .firstPerfect: return "sparkles"
        case .streakMilestone: return "flame.fill"
        case .levelCountMilestone: return "checkmark.seal.fill"
        default: return "star.fill"
        }
    }

    private var titleKey: String {
        switch event {
        case .firstPerfect: return "toast.firstPerfect.title"
        case .streakMilestone(let d): return "toast.streak\(d).title"
        case .levelCountMilestone(let c): return "toast.levels\(c).title"
        default: return "toast.generic.title"
        }
    }

    private var subtitleKey: String {
        switch event {
        case .firstPerfect: return "toast.firstPerfect.subtitle"
        case .streakMilestone(let d): return "toast.streak\(d).subtitle"
        case .levelCountMilestone(let c): return "toast.levels\(c).subtitle"
        default: return "toast.generic.subtitle"
        }
    }
}

private extension String {
    var localized: String { NSLocalizedString(self, comment: "") }
}
```

- [ ] **Step 2: MilestoneToastHost 実装 (キュー管理 + 8秒上限)**

```swift
import SwiftUI

struct MilestoneToastHost: View {
    @EnvironmentObject var coordinator: CelebrationCoordinator
    @State private var isVisible = false
    @State private var dismissTask: Task<Void, Never>?
    @State private var consumedSeconds: Double = 0

    var body: some View {
        VStack {
            if let event = coordinator.currentToast {
                MilestoneToastView(event: event) {
                    dismissNow()
                }
                .transition(.move(edge: .top).combined(with: .opacity))
                .padding(.top, 12)
                .onAppear {
                    scheduleDismiss(for: event)
                }
                .onChange(of: event) { _, new in
                    scheduleDismiss(for: new)
                }
            }
            Spacer()
        }
        .allowsHitTesting(coordinator.currentToast != nil)
        .animation(.easeInOut(duration: 0.3), value: coordinator.currentToast)
    }

    private func scheduleDismiss(for event: CelebrationEvent) {
        dismissTask?.cancel()
        let duration: Double
        switch event {
        case .firstPerfect: duration = 4.0
        case .streakMilestone: duration = 3.5
        case .levelCountMilestone: duration = 3.0
        default: duration = 3.0
        }
        let remaining = max(0.0, 8.0 - consumedSeconds)
        let effective = min(duration, remaining)
        guard effective > 0 else {
            // 上限超過 — 残りキューを全部 dismiss (seen は marked 済み)
            coordinator.dismissCurrent()
            return
        }
        consumedSeconds += effective
        dismissTask = Task {
            try? await Task.sleep(nanoseconds: UInt64(effective * 1_000_000_000))
            await MainActor.run {
                coordinator.dismissCurrent()
                if coordinator.currentToast == nil {
                    consumedSeconds = 0
                }
            }
        }
    }

    private func dismissNow() {
        dismissTask?.cancel()
        coordinator.dismissCurrent()
        if coordinator.currentToast == nil { consumedSeconds = 0 }
    }
}
```

- [ ] **Step 3: AppView ルートに overlay 追加**

```swift
.overlay {
    MilestoneToastHost()
        .environmentObject(coordinator)
}
```

- [ ] **Step 4: ローカライズキー追加**

トースト各種の title/subtitle (firstPerfect, streak7/14/30, levels10/50/100) の 7 グループ。

- [ ] **Step 5: ビルド + 目視 (DebugMenu から発火)**

- [ ] **Step 6: コミット**

```bash
git add PixelColoringGame/Features/Celebration/MilestoneToastView.swift \
        PixelColoringGame/Features/Celebration/MilestoneToastHost.swift \
        PixelColoringGame/App/AppView.swift \
        PixelColoringGame/Resources/Localizable.xcstrings
git commit -m "feat(M3): MilestoneToastHost with 8s queue cap"
```

### Task M3.4: DailyExperience.commitToday から trigger 発火

**Files:**
- Modify: `PixelColoringGame/Core/DailyExperience.swift`

仕様書 §4: `DailyExperience.commitToday` 末尾。

- [ ] **Step 1: commitToday を Read**

profile, snapshot へのアクセスがあるか確認。

- [ ] **Step 2: trigger 発火**

```swift
let snapshot = profileStore.makeSnapshot(
    profile: profile,
    lastLevelChapterID: nil,
    lastLevelWasPerfect: lastWasPerfect,
    lastLevelWasChapterFinalLevel: false,
    isJourneyComplete: false,
    monthlyCompletedYearMonth: monthlyCompletedYM  // 31/31 達成時のみ non-nil
)
coordinator.handle(trigger: .dailyCompleted(profileSnapshot: snapshot))
```

DailyExperience が coordinator にアクセスできるよう、イニシャライザに追加 or 呼び出し側で渡す。

- [ ] **Step 3: 既存テスト調整**

`DailyExperienceTests.swift` で coordinator dummy を渡せる構造に。

- [ ] **Step 4: ビルド + テスト**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

- [ ] **Step 5: コミット**

```bash
git add PixelColoringGame/Core/DailyExperience.swift \
        PixelColoringGameTests/DailyExperienceTests.swift
git commit -m "feat(M3): DailyExperience.commitToday fires celebration trigger"
```

### Task M3.5: appLaunched 検知 (streak のみ)

**Files:**
- Modify: `PixelColoringGame/App/AppView.swift`

仕様書 §4: `AppView` の app launch 検知。

- [ ] **Step 1: AppView の `.onAppear` または `hasStarted` 遷移後 1 回限りのフックを追加**

```swift
.onChange(of: hasStarted) { _, isStarted in
    guard isStarted, !hasLaunchedTrigger else { return }
    hasLaunchedTrigger = true
    let snapshot = playerProfileStore.makeSnapshot(
        profile: profile,
        lastLevelChapterID: nil,
        lastLevelWasPerfect: false,
        lastLevelWasChapterFinalLevel: false,
        isJourneyComplete: false,
        monthlyCompletedYearMonth: nil
    )
    celebrationCoordinator?.handle(trigger: .appLaunched(profileSnapshot: snapshot))
}
```

`@State private var hasLaunchedTrigger = false` を追加。

- [ ] **Step 2: ビルド**

- [ ] **Step 3: DebugMenu で streak=7 にセット → kill → 再起動 → トースト確認**

DebugMenu に「Set streakDays = N」機能を追加 (M3.7 のスコープ)。本タスクでは仕組みのみ。

- [ ] **Step 4: コミット**

```bash
git add PixelColoringGame/App/AppView.swift
git commit -m "feat(M3): app launch trigger detects streak milestones"
```

### Task M3.6: DebugMenu 拡張

**Files:**
- Modify: `PixelColoringGame/Features/Debug/DebugMenuView.swift`

仕様書 §4「DebugMenu」: ストリーク日数セット、累計レベル数セット、初Perfect リセット等。

- [ ] **Step 1: PlayerProfile の編集セクションを追加**

DebugMenuView に:

```swift
Section("Profile fields") {
    Stepper("Streak: \(profileStreak)", value: Binding(
        get: { profileStreak },
        set: { newValue in setStreak(newValue) }
    ), in: 0...90)

    Stepper("Levels completed: \(profileLevels)", value: Binding(
        get: { profileLevels },
        set: { newValue in setLevels(newValue) }
    ), in: 0...200)

    Button("Reset firstPerfect (clear seenKey)") {
        clearSeen("firstPerfect")
    }
}
```

helper:

```swift
private var profileStreak: Int { profiles.first?.currentStreak ?? 0 }
private var profileLevels: Int { (profiles.first?.completedLevelIDs.count) ?? 0 }
```

実フィールド名 (completedLevelIDs 等) は実装時に確定。

- [ ] **Step 2: 破壊的操作に確認ダイアログ**

「Clear all celebrationsSeen」に `.confirmationDialog` を付与。

- [ ] **Step 3: ビルド**

- [ ] **Step 4: コミット**

```bash
git add PixelColoringGame/Features/Debug/DebugMenuView.swift
git commit -m "feat(M3): DebugMenu profile field editors + destructive confirmations"
```

### Task M3.7: M3 完了確認 (シナリオ 1-4 実機検証)

仕様書 §10 M3 検証 を全項目実施。

- [ ] **Step 1: 全テスト green**

```bash
xcodebuild test -scheme PixelColoringGame -project PixelColoringGame.xcodeproj \
  -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 15' \
  -derivedDataPath /tmp/PixelColoringGameDerived CODE_SIGNING_ALLOWED=NO 2>&1 | tail -20
```

- [ ] **Step 2: シナリオ 2 (Journey 吸収) 実機検証**

DebugMenu で `journeyComplete` 候補状態を作る → 章最終レベルクリア → JourneyCompleteView のみ表示、配下が出ないことを確認。

- [ ] **Step 3: シナリオ 3 (appLaunched streak) 検証**

DebugMenu で streakDays=7 → kill → 再起動 → `streak:7` トースト表示確認。

- [ ] **Step 4: シナリオ 4 (Monthly完走 CTA 切替) 検証**

DebugMenu で monthly 31/31 → `MonthlyCompleteView` 表示、CTA が「Continue」or「Next Month →」になることを確認 (現実の dailyRepository の状態に依存)。

- [ ] **Step 5: Reduce Motion / Low Power 全祝福確認**

設定で Reduce Motion ON、もしくは低電力モード ON で各祝福を一通り発火し、破綻なし。

- [ ] **Step 6: パフォーマンス計測**

```
Instruments で PixelBoardView at 4.0x zoom → 60fps 維持
ParticleEmitterView 稼働中 → CPU 30% 未満 (iPhone SE 2nd)
```

実機が無ければシミュレータの数値で代替し、要観察項目として記録。

- [ ] **Step 7: M3 tag + Phase 1 完了 tag**

```bash
git tag m3-complete
git tag phase1-complete
git log --oneline -20
```

---

## Phase 1 完了条件 (まとめ)

- [ ] M1: 触覚 5 段階化、セル pop、パレット glow、ヒント pulse、Reduce Motion 動作確認
- [ ] M2: CelebrationEvent/Detector/Coordinator が全テスト green、ChapterClearView が DebugMenu と実プレイの両方で動作、SwiftData マイグレーション実機 OK
- [ ] M3: JourneyCompleteView/MonthlyCompleteView/MilestoneToastHost が動作、シナリオ 1-4 実機 OK、Reduce Motion / Low Power 全演出確認
- [ ] パフォーマンス: 60fps、ParticleEmitter CPU 30% 未満
- [ ] 全テスト green

完了後、`phase1-complete` tag を打って、Phase 2 のプラン作成に着手する。
